'use strict';

describe('membership module', function() {

  beforeEach(module('project.membership'));

  describe('membership controller', function() {

    it('should ....', inject(function($controller) {
      //spec body
      var MembershipController = $controller('MembershipController');
      expect(MembershipController).toBeDefined();
    }));

  });
});
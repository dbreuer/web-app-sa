'use strict';

describe('qualifications module', function() {

  beforeEach(module('project.qualifications'));

  describe('qualifications controller', function() {
    //console.log('qualifications HTTP:\r\n');
    it('should ....', inject(function($controller) {
      //spec body
      var QualificationsController = $controller('QualificationsController');
      expect(QualificationsController).toBeDefined();
    }));

  });
});
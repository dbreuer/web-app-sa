'use strict';

describe('myaat module', function() {

  beforeEach(module('project.myaat'));

  describe('myaat controller', function() {

    it('should ....', inject(function($controller) {
      //spec body
      var MyaatController = $controller('MyaatController');
      expect(MyaatController).toBeDefined();
    }));

  });
});
'use strict';

describe('project.maintenance module', function() {

  beforeEach(module('project.maintenance'));

  describe('maintenance controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var MaintenanceCtrl = $controller('MaintenanceCtrl');
      expect(MaintenanceCtrl).toBeDefined();
    }));

  });
});